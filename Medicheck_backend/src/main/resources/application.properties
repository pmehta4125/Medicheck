spring.application.name=medicheck_backend
server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/medicheck
spring.datasource.username=root
spring.datasource.password=Pmehta@123
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
jwt.secret=105604480db86a6a5da69e7ef7a9a59c
# Comma-separated admin emails allowed to access /admin features.
app.admin.emails=admin@medicheck.com
google.client-id=795196955520-7045fhj5ou53l71p6ee8tpbql4niend9.apps.googleusercontent.com
# Do not hardcode API keys here. Keep them in environment variables.
ocr.cloud.google.api-key=${GOOGLE_VISION_API_KEY:}
# Separate Gemini API key (get free from https://aistudio.google.com/apikey)
# If empty, falls back to ocr.cloud.google.api-key
gemini.api-key=${GEMINI_API_KEY:}
# Backup Gemini API key — used automatically when the primary key hits 429 quota limit
# Get a free key from https://aistudio.google.com/apikey
gemini.api-key-backup=${GEMINI_API_KEY_BACKUP:}
ocr.cloud.google.min-local-score=185
ocr.cloud.force-primary=true
ocr.cloud.compare-with-local=true
ocr.mock.enabled=false

# Gemini timeout/retry tuning (images are large, need longer timeout)
gemini.http.connect-timeout-ms=15000
gemini.http.read-timeout-ms=60000
gemini.auth-failure-cooldown-ms=120000

# PostgreSQL (Render) — uncomment for deployment
#spring.datasource.url=jdbc:postgresql://medicheck_db_an5p_user:mKcmC6vMNuMAi64eq0cjcBST03oJTmTB@dpg-d77mam14bi0s73f0orag-a/medicheck_db_an5p
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

file.upload-dir=uploads/